// A simple model to represent a scheduled time slot for checking vitals.
class VitalTimeslot {
  final String id;
  final String label;

  const VitalTimeslot({
    required this.id,
    required this.label,
  });
}
